// Phase 1: Query function stubs.
// Full query implementations are added in Phase 2 (worker) and Phase 3 (frontend).
// Each query function will live in its own file in this directory.
// Example structure (to be created):
//   ./matches.ts     — getRecentMatches, getMatchById
//   ./players.ts     — getRoster, getPlayerById, getPlayerGameLog
//   ./club.ts        — getClubStats
//   ./seasons.ts     — listContentSeasons
//   ./health.ts      — getLastSuccessfulIngestion

export {}
