# Tier 0 — Quarantined worker tests (pre-existing live-data-drift failures)
#
# These worker integration tests were ALREADY RED before the Tier 0 quality-gate
# work (verified by running the suite at commit b17f983 with the 0.1 changes
# stashed: 7 failures, identical to the set below). They fail because the LIVE
# database's match-250 / 463 canonical data has drifted away from the values the
# tests + frozen regression-floor fixtures encode — NOT because of any Tier 0
# code change.
#
# Decision (user, 2026-06-13): ship the activate quality gate now and QUARANTINE
# these as a Tier 1 data-repair backlog item, rather than (a) pull a live re-OCR
# / re-consolidate into Tier 0, or (b) lower the V2 ground-truth expectations to
# match the degraded data (which would be dishonest for a "verified" baseline).
#
# Evidence of the drift:
#   - match 250 L2 (BGM actor resolution) is 85.42% live vs a frozen floor of
#     97.92% (the "layer scores at or above floor" regression test). The actor
#     resolution rate regressed at some earlier reprocessing.
#   - match 250 `bgm/LW: captain` is `true` in the live DB but V2 says `false`
#     (a stale is_captain OR-vote leak) — the getMatchLineups / lobby-field
#     benchmark assertions.
#
# Tier 1 follow-up: investigate + repair match-250 (and 463) canonical data
# (re-OCR / re-consolidate / backfill-event-actor-resolution), then re-anchor
# the L2 threshold on the restored scores and DELETE the matching lines here.
#
# verify-ocr.sh treats the worker suite as GREEN iff its set of failing tests is
# a SUBSET of the names below. Any test name not listed here that fails is a
# real regression and blocks the gate. Match is by exact subtest name (one per
# line; blank lines + `#` comments ignored).

match 250: getMatchLineups returns expected slot data
match 250: pre-game lobby BGM loadout fields match V2
match 250: lobby typed_v1 hard-field accuracy ≥ 90%
match 250: lobby typed_v1 soft-field accuracy ≥ 75%
match 250 — layer scores at or above floor
match 463 — layer scores at or above floor
computeLayers reproduces regression-floor layers for match 463
