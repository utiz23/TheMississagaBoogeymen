# Phase 4: Full multi-stage production build will be added here.
# This placeholder satisfies docker-compose.yml during Phase 0 scaffolding.
FROM node:22-alpine
WORKDIR /app
CMD ["echo", "Web not yet built — run after Phase 3 implementation"]
