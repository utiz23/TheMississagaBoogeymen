# Dependencies
node_modules

# Brainstorm session files
.superpowers/

# Build outputs
dist
build
.next

# Environment
.env
.env.local
.env.*.local

# Turbo
.turbo

# Misc
*.tsbuildinfo
.DS_Store

# Python
.venv/
.venv-*/
__pycache__/

# OCR tool — screenshots are large binary assets, not source code
tools/game_ocr/ScreenShots/

# Playwright MCP cache
.playwright-mcp/

# Claude session state (settings.json IS tracked; only the lock file is ephemeral)
.claude/scheduled_tasks.lock
.claude/worktrees/

# Historical extractor outputs — regenerable, multi-GB, never belong in git
tools/historical_import/output_*/

# Stray pip stdout files (e.g. accidental "pip install x>=1.3" → file named =1.3)
/=*

# Debug HTML scrapes from local development
/roster-main-*.html

# Debug screenshots and snapshot dumps at repo root (Playwright/Chrome DevTools output)
/*.png
/*.txt

# Bin for those root-level screenshots — non-destructive holding area
.local-screenshots/

# Accidental browser-download duplicates ("foo (1).ext", "foo (2).ext")
* (1).*
* (2).*

# Design system hand-off zips (extracted to docs/design/boogeymen-system/ as needed)
*.zip

# Staged design system reference (source-of-truth lives in apps/web; this is local working copy)
docs/design/boogeymen-system/

# Research artifacts — large captures not suited for git
research/**/*.har
research/**/*_files/
research/Previous_NHL_Stats/

# OCR source screenshots — large binary capture set, kept on disk only.
# Exception: the small markdown ground-truth / benchmark files are the
# canonical regression baseline (read by tools/game_ocr benchmark scripts),
# so they are version-controlled; only the media subdirs stay ignored.
research/OCR-SS/*
!research/OCR-SS/*.md

# CVAT labeling workspace — multi-hundred-MB local annotation data
cvat/

# Label Studio (local labeling tool) — per-developer state + raw inbox + export JSON
.label-studio-data/
tools/game_ocr/calibration/extras/_inbox/
label-studio-export-*.json
# Also ignore the unsuffixed canonical export — labels live in the local
# Label Studio instance, not in git. The file grows fast (1.5MB+) and its
# format flips between single-line and pretty-printed across exports.
/label-studio-export.json

# Loose screen-classifier debug PNGs emitted by Pass-1 to calibration/extras/
# at run time. Tracked fixtures live in extras/loadout/fixtures/ and stay tracked.
tools/game_ocr/calibration/extras/*.png

# Stray one-off probe scripts that landed under src/ by mistake
apps/web/src/probe-queries.mjs

# Local OCR pipeline backup snapshots — do not commit.
.tmp-backups/

# Phase 3a diagnostic TSVs (regenerate via scripts/diagnose_segments.py)
tools/game_ocr/diagnostics/
